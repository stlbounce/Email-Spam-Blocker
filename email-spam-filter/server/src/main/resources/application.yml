spring:
  datasource:
    url: jdbc:mysql://localhost:3306/spamfilter?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: Andre622@
  jpa:
    hibernate:
      ddl-auto: update   # good for development; use migrations later in prod
    show-sql: true
    properties:
      hibernate.format_sql: true

server:
  port: 8080
